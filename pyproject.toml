[project]
name = "krea2-lora-merge"
version = "0.1.0"
description = "Merge weighted Krea 2 LoRAs, including mixed ranks and naming formats"
requires-python = ">=3.10"
dependencies = [
    "numpy>=2.0",
]

[project.scripts]
krea2-lora-merge = "merge_krea2_loras:main"

[tool.uv]
package = false

